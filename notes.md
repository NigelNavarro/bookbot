# notes.md

## stats.py

- Remember that dictionaries have methods like .items(), .keys(), and .values()
- .items() returns a list of tuples (each key-value pair)
- char_dict.keys() returns just the keys
- char_dict.values() returns just the values